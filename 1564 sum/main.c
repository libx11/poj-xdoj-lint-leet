#include <stdio.h>
#include <stdlib.h>
#define MAX 15



int main()
{
    int i, j, sum, n, temp, num[MAX];

    while(scanf("%d%d", &sum, &n), sum != 0 && n != 0)
    {
        for(i = 0; i < n; i++)
            scanf("%d", &num[i]);


    }
    printf("Hello world!\n");
    return 0;
}
